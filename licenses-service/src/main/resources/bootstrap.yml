spring:
  application:
    name: licenseservice
  profiles:
    active:
      default
  cloud:
    config:
      uri: http://localhost:8888
  datasource:
    initialization-mode: never
ribbon:
  ConnectTimeout: 5000
  ReadTimeout: 5000